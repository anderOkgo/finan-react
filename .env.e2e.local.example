# Copy to .env.e2e.local (gitignored, covered by *.local) and fill in real
# values. The whole E2E suite requires this -- unlike animecream-react,
# finan-react has no public/unauthenticated view (Home shows the login
# form directly whenever no user is logged in).

E2E_LOGIN=
E2E_PASSWORD=
